apply plugin: 'com.deploygate.plugins.sdk'

sdk {
    displayName.set("DeployGate SDK Mock")
    artifactId.set('sdk-mock')
    description.set('Mocked dummy DeployGate SDK for Android to reduce footprint of your release version app without code modification')
}
